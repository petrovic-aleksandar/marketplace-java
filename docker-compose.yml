services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: My01pass
      POSTGRES_DB: marketplace
    hostname: dbhost
  
  wf:
     build:
       context: .
       dockerfile: Dockerfile
     ports:
      - 8080:8080
      - 9990:9990